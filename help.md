

在终端启动时：
  claude --resume
  打开交互式会话选择器，显示所有历史会话（名称、时间、消息数等）。

  在当前会话内：
  /resume
  打开同样的选择器，可以切换到其他会话。

  ---
  重命名会话

  在会话内随时重命名：
  /rename 我的会话名称

  启动时直接命名：
  claude -n "相册项目"

  在选择器里重命名： 用 /resume 打开选择器 → 选中目标会话 → 按 R 键重命名。

  ---
  重新进入会话

  ┌────────────────────────┬──────────────────────────┐
  │          命令          │           用途           │
  ├────────────────────────┼──────────────────────────┤
  │ claude --continue      │ 继续当前目录最近的会话   │
  ├────────────────────────┼──────────────────────────┤
  │ claude --resume        │ 打开选择器，选择任意会话 │
  ├────────────────────────┼──────────────────────────┤
  │ claude --resume 会话名 │ 直接进入指定名称的会话   │
  └────────────────────────┴──────────────────────────┘

  ---
  选择器快捷键

  ┌────────┬─────────────────────────────┐
  │ 快捷键 │            功能             │
  ├────────┼─────────────────────────────┤
  │ ↑ / ↓  │ 上下选择                    │
  ├────────┼─────────────────────────────┤
  │ Enter  │ 进入选中会话                │
  ├────────┼─────────────────────────────┤
  │ R      │ 重命名                      │
  ├────────┼─────────────────────────────┤
  │ /      │ 搜索过滤                    │
  ├────────┼─────────────────────────────┤
  │ A      │ 切换显示全部项目 / 当前目录 │
  ├────────┼─────────────────────────────┤
  │ Esc    │ 退出选择器                  │
  └────────┴─────────────────────────────┘